apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "egokey.fullname" . }}
  labels:
    {{- include "egokey.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "egokey.selectorLabels" . | nindent 6 }}
  replicas: 1
  template:
    metadata:
      labels:
        {{- include "egokey.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: egokey
          image: {{ .Values.image }}
          env:
            - name: NODE_ENV
              value: {{ .Values.environment }}
          volumeMounts:
            - name: {{ include "egokey.fullname" . }}-configuration
              mountPath: /cherrypick/.config
              readOnly: true
          ports:
            - containerPort: 3000
        - name: postgres
          image: postgres:18-alpine
          env:
            - name: POSTGRES_USER
              value: "example-egokey-user"
            - name: POSTGRES_PASSWORD
              value: "example-egokey-pass"
            - name: POSTGRES_DB
              value: "cherrypick"
          ports:
            - containerPort: 5432
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
      volumes:
        - name: {{ include "egokey.fullname" . }}-configuration
          configMap:
            name: {{ include "egokey.fullname" . }}-configuration
