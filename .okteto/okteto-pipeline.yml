build:
  egokey:
    args:
      - NODE_ENV=development
deploy:
  - helm upgrade --install egokey chart --set image=${OKTETO_BUILD_EGOKEY_IMAGE} --set url="https://egokey-$(kubectl config view --minify -o jsonpath='{..namespace}').cloud.okteto.net" --set environment=development
